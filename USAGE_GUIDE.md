# UDP示波器使用指南

## 专业示波器操作说明

### 缩放和偏移操作

#### 水平缩放（时基调节）
- **鼠标滚轮**：在示波器显示区域滚动鼠标滚轮可调节水平时基
  - 向上滚动：时基减小，显示更短的时间窗口（放大）
  - 向下滚动：时基增大，显示更长的时间窗口（缩小）
- **UI控件同步**：右侧时基设置spinbox会自动更新
- **配置保存**：时基变化会自动保存到配置文件

#### 水平偏移（时间偏移）
- **鼠标拖拽**：按住鼠标左键水平拖拽可调节时间偏移
  - 只允许水平方向偏移，垂直方向被禁用
  - 向左拖拽：时间轴向左移动
  - 向右拖拽：时间轴向右移动
- **UI控件同步**：右侧时间偏移设置spinbox会自动更新
- **配置保存**：偏移变化会自动保存到配置文件

#### 垂直缩放（通道挡位调节）
- **Ctrl + 鼠标滚轮**：按住Ctrl键同时滚动鼠标滚轮调节当前通道的垂直挡位
  - 向上滚动：挡位减小，波形放大
  - 向下滚动：挡位增大，波形缩小
- **作用范围**：只影响当前选中通道的显示，不影响坐标系
- **UI控件同步**：对应通道的垂直挡位spinbox会自动更新
- **配置保存**：挡位变化会自动保存到配置文件

#### 垂直偏移（通道偏移调节）
- **Ctrl + 鼠标拖拽**：按住Ctrl键同时按住鼠标左键上下拖拽调节当前通道的垂直偏移
  - 向上拖拽：波形向上偏移
  - 向下拖拽：波形向下偏移
- **作用范围**：只影响当前选中通道的显示，不影响坐标系
- **UI控件同步**：对应通道的垂直偏移spinbox会自动更新
- **配置保存**：偏移变化会自动保存到配置文件

### 通道管理

#### 通道选择
- **下拉框选择**：使用右侧的通道选择下拉框切换当前操作的通道
- **当前通道**：所有垂直缩放和偏移操作都作用于当前选中的通道

#### 通道显示开关
- **快速开关**：主界面左侧的通道显示按钮组可快速开启/关闭通道显示
- **颜色指示**：按钮显示对应通道的颜色
- **状态同步**：按钮状态与通道配置自动同步

#### 通道配置
- **颜色设置**：点击颜色按钮可更改通道颜色
- **单位设置**：可设置通道的测量单位（V、A、W、Hz、rpm、°C等）
- **缩放比例**：可设置通道的数据缩放比例
- **统计信息**：实时显示最小值、最大值、平均值、RMS值

### 滚动模式

#### 自动滚动
- **启用方式**：勾选"自动滚动"单选按钮
- **显示特点**：
  - 新数据点从右侧出现
  - X轴原点固定在最右侧（X=0）
  - 旧数据从左侧消失
  - X坐标都是负值，表示相对于当前时刻的时间

#### 暂停滚动
- **暂停方式**：取消勾选"自动滚动"单选按钮
- **观察模式**：
  - 可自由调节水平偏移观察历史数据
  - 后台继续接收和缓存数据
  - 可使用鼠标拖拽浏览不同时间段的数据

### 光标测量

#### 光标启用
- **启用方式**：在通道配置页面勾选"启用光标"
- **光标类型**：
  - X1、X2：垂直光标线，用于时间测量
  - Y1、Y2：水平光标线，用于幅值测量

#### 测量结果
- **ΔX**：两个X光标之间的时间差
- **ΔY**：两个Y光标之间的幅值差
- **频率**：根据ΔX计算的频率值（1/ΔX）

#### 光标操作
- **拖拽调节**：可直接拖拽光标线调节位置
- **数值输入**：可在右侧输入框中精确设置光标位置

### 配置管理

#### 采样频率
- **设置位置**：配置文件中的`display.sample_rate`
- **作用**：影响时间轴的正确计算和光标测量精度
- **默认值**：1000 Hz

#### 配置文件
- **位置**：项目根目录的`config.json`
- **热重载**：点击"重新加载配置"按钮可重新加载配置
- **自动保存**：所有操作产生的配置变化都会自动保存

#### 默认配置
- **位置**：`src/config/default_config.json`
- **作用**：当用户配置文件不存在或损坏时的备用配置
- **通道数量**：默认10个通道，可根据需要修改

### 性能优化

#### OpenGL加速
- **自动启用**：程序自动启用OpenGL硬件加速
- **性能提升**：显著提高绘图性能，支持120fps刷新率
- **兼容性**：需要显卡支持OpenGL

#### 内存管理
- **环形缓冲区**：使用高效的环形缓冲区管理数据
- **存储深度**：默认1GB内存缓存，可在配置文件中调整
- **自动清理**：超过存储深度时自动丢弃最早的数据

### 故障排除

#### 常见问题
1. **鼠标操作无响应**
   - 确保鼠标在示波器显示区域内
   - 检查是否正确按住Ctrl键（垂直操作）

2. **配置不保存**
   - 检查程序是否有写入权限
   - 查看日志文件中的错误信息

3. **显示卡顿**
   - 检查OpenGL支持
   - 降低刷新率设置
   - 减少显示点数

4. **数据接收异常**
   - 检查UDP端口是否被占用
   - 确认网络连接正常
   - 查看通信统计信息

#### 日志文件
- **位置**：项目根目录的`oscilloscope.log`
- **内容**：包含详细的运行日志和错误信息
- **调试**：遇到问题时可查看日志获取详细信息

### 快捷键总结

| 操作 | 快捷键/鼠标操作 | 说明 |
|------|----------------|------|
| 水平缩放 | 鼠标滚轮 | 调节时基挡位 |
| 水平偏移 | 鼠标左键拖拽 | 只允许水平方向 |
| 垂直缩放 | Ctrl + 鼠标滚轮 | 当前通道挡位 |
| 垂直偏移 | Ctrl + 鼠标左键拖拽 | 当前通道偏移 |
| 通道开关 | 点击通道按钮 | 快速开关显示 |
| 光标拖拽 | 直接拖拽光标线 | 调节光标位置 |

### 技术规格

- **刷新率**：最高120fps
- **通道数量**：10个（可配置）
- **数据格式**：支持uint16和float32
- **存储深度**：1GB（可配置）
- **采样频率**：1000Hz（可配置）
- **时基范围**：0.001ms - 1000ms
- **垂直挡位**：0.001V - 1000V
